plugins {
    `java-library`
}

allprojects {
    group = "com.github.ljtfreitas.julian-http-client.samples"
    version = "0.0.1-SNAPSHOT"

    repositories {
        mavenLocal()
        mavenCentral()
    }
}

description = "julian-http-client samples"
